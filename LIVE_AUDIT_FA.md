# گزارش آماده‌سازی Kebabest برای انتشار

## تغییرات انجام‌شده

- پرداخت آنلاین از مسیر مشتری حذف شد.
- سفارش شامل نام، موبایل، آدرس، یادداشت، اقلام، تعداد، مالیات، هزینه ارسال و جمع کل در واتساپ صاحب رستوران باز می‌شود.
- مبلغ سفارش در سرور و بر اساس منوی ثبت‌شده محاسبه می‌شود؛ مبلغ ارسالی از مرورگر قابل اعتماد نیست.
- سفارش پیش از بازشدن واتساپ در سرور ثبت می‌شود و در پنل ادمین دیده می‌شود.
- شماره واتساپ با کد کشور از پنل ادمین قابل تغییر است.
- رمز و PIN ادمین از کد عمومی حذف و به متغیرهای امن سرور منتقل شد.
- نشست ادمین با Cookie امن و HttpOnly نگهداری می‌شود.
- منو، تنظیمات، سطل زباله و سفارش‌ها از localStorage به ذخیره‌سازی سمت سرور منتقل شد تا بین دستگاه‌ها مشترک باشند.
- متن‌های مربوط به پرداخت موفق، درگاه آماده‌نشده، دسترسی آزمایشی و ادعاهای غیرواقعی از رابط حذف شد.
- هدر موبایل، بخش اطلاعات تماس و دسته‌بندی‌های موبایل بازطراحی شدند؛ خطوط جداکننده حذف و کارت‌های مستقل جایگزین شدند.
- ردیف افقی دسته‌بندی منو در موبایل با انتخاب‌گر دسته‌بندی جایگزین شد.
- هدرهای امنیتی پایه اضافه شدند و هدر معرفی Next.js حذف شد.

## امکانات پنل ادمین

- مشاهده خودکار سفارش‌های جدید با تازه‌سازی هر ۱۰ ثانیه
- اعلان مرورگر برای سفارش تازه در صورت صدور مجوز
- وضعیت‌های سفارش: جدید، پذیرفته‌شده، آماده، ارسال‌شده، تکمیل‌شده، ردشده و سطل زباله
- کم‌شدن موجودی هنگام قبول سفارش و بازگشت موجودی در صورت رد سفارش
- افزودن، ویرایش، تکثیر، غیرفعال‌کردن، حذف موقت، بازیابی و حذف دائمی غذای منو
- تغییر قیمت، موجودی، دسته‌بندی، توضیح، Veg/Non-veg و Popular
- فشرده‌سازی و ثبت عکس غذا
- تغییر نام رستوران، تلفن، شماره واتساپ، آدرس، ساعات کاری، GST، هزینه تقریبی ارسال و متن‌های سفارش
- مدیریت لینک‌های ویدیویی

## تست‌های انجام‌شده

- Production build: موفق
- TypeScript check: موفق
- بررسی امنیت وابستگی‌ها: بدون آسیب‌پذیری گزارش‌شده
- دریافت منوی عمومی: موفق، ۲۰ آیتم اولیه
- ایجاد سفارش واقعی در API: موفق
- محاسبه مجدد قیمت در سرور و جلوگیری از دست‌کاری مبلغ: موفق
- ورود ادمین با Cookie سمت سرور: موفق
- جلوگیری از دسترسی بدون ورود: پاسخ 401
- قبول سفارش و کم‌شدن موجودی: موفق
- رد سفارش پذیرفته‌شده و بازگشت موجودی: موفق
- هدرهای امنیتی پایه: فعال

## موارد ضروری پیش از انتشار عمومی

1. پروژه باید روی VPS یا میزبان Node.js دارای فضای دائمی و قابل‌نوشتن اجرا شود. ذخیره‌سازی فایل JSON برای Vercel مناسب نیست.
2. در `.env` مقادیر قوی برای `ADMIN_PASSWORD`، `ADMIN_PIN` و `ADMIN_SESSION_SECRET` ثبت شود.
3. `KEBABEST_DATA_DIR` به یک مسیر دائمی خارج از پوشه انتشار تنظیم و روزانه پشتیبان‌گیری شود.
4. دامنه، HTTPS، Nginx، PM2 یا systemd و راه‌اندازی خودکار پس از ری‌استارت سرور تنظیم شوند.
5. شماره واتساپ با کد کشور، آدرس، قیمت‌ها، GST، هزینه ارسال، ساعات کاری و تصاویر نهایی بررسی شوند.
6. یک سفارش از موبایل و اینترنت جداگانه ثبت شود و رسیدن آن به واتساپ و پنل ادمین کنترل شود.
7. واتساپ اجازه ارسال خودکار پیام را نمی‌دهد؛ صفحه فقط پیام کامل را باز می‌کند و مشتری باید دکمه Send را در واتساپ بزند.
8. برای ترافیک بالاتر، ذخیره‌سازی JSON باید با PostgreSQL/Supabase جایگزین و Rate Limit و ضداسپم اضافه شود.
9. ظاهر نهایی روی چند گوشی واقعی، به‌خصوص عرض‌های 360، 390 و 430 پیکسل، یک بار کنترل شود.
